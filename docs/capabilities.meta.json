{
  "_comment": "Hand-curated layer of docs/capabilities.json (capability-config.md §2 p.3). The emitter (stapel_chat/_capabilities.py) merges this over the derived axes. Chat exposes three CTO-facing config axes — CHAT_KINDS, ATTACHMENTS, MAX_BODY_LENGTH — curated below; SCOPE_PROVIDER is an extension seam. Keep language business-facing — it surfaces in CTO conversations, not in code. The `surface` section is the third one (stapel_tools.surface): the domain calls a product is meant to make instead of writing its own — surface_roots select the symbols, and every selected symbol MUST carry an intent line here or emission fails naming it.",
  "provides": "Conversations and messaging: direct (1:1, idempotent by participant pair), group and support threads on one model; text/system messages with a monotonic per-conversation seq, replies and opaque CDN attachment keys; per-participant read markers and unread counts; anchor-paginated history (canonical anchor=seq) and conversation lists; a support layer (unassigned queue, first-come assignment, open/pending/resolved with reopen); and an optional Channels consumer for realtime delivery with seq-replay resume.",
  "requires": [
    {
      "module": "stapel-core",
      "optional": false,
      "reason": "comm bus (chat.message / chat.support.assigned emits via the outbox), JWT authentication (HTTP + optional Channels), AppSettings config layer, anchor pagination"
    },
    {
      "module": "stapel-auth",
      "optional": true,
      "reason": "every endpoint requires an authenticated user (IsAuthenticated); stapel-auth is the shelf's session issuer — any stapel-core-compatible JWT issuer satisfies the check"
    },
    {
      "module": "stapel-cdn",
      "optional": true,
      "reason": "messages carry opaque attachment keys only; a file/CDN module (or any host storage) owns the bytes those keys point at"
    }
  ],
  "extension_points": [
    {
      "name": "SCOPE_PROVIDER",
      "kind": "dotted_path",
      "summary": "Swap how the opaque scope_key (workspace/org/tenant) is resolved from the request and enforced on querysets — the default is a single global scope; a host may return the active workspace_id so conversations are partitioned per tenant."
    },
    {
      "name": "chat.message",
      "kind": "comm_event",
      "summary": "Emitted (transactionally, via the outbox) whenever a message is appended. Realtime delivery, search indexing and notifications subscribe without any coupling in the engine."
    },
    {
      "name": "chat.support.assigned",
      "kind": "comm_event",
      "summary": "Emitted when a support conversation is claimed by an operator — routing and operator-notification layers subscribe."
    },
    {
      "name": "serializer_seams",
      "kind": "class_override",
      "summary": "Every view declares request/response serializer seams (SerializerSeamMixin) — subclass the view, override the attribute, remount the URL."
    }
  ],
  "surface_roots": [
    {
      "select": "functions",
      "path": "services.py"
    }
  ],
  "surface": {
    "create_direct": {
      "kind": "factory",
      "intent": "Get-or-create the 1:1 thread between two users — idempotent by an order-independent direct_key over (scope_key, both ids), with the create race decided by the partial unique constraint (the loser returns the winner's row). Reach for this instead of your own get_or_create over a participant pair: a hand-rolled one has nothing unique to key on and quietly hands the two users two different threads.",
      "instead_of": [
        "stapel_chat.models.Conversation.objects.get_or_create"
      ]
    },
    "create_group": {
      "kind": "factory",
      "intent": "Create a group thread and seed its participant rows in the same call (owner first, ids deduped, conflicts ignored). Group threads are deliberately never deduplicated — each call is a new conversation. A Conversation row created by hand has no participants, and the list endpoint filters on participants__user, so nobody can see it.",
      "instead_of": [
        "stapel_chat.models.Conversation.objects.create"
      ]
    },
    "create_support": {
      "kind": "factory",
      "intent": "Open a support thread for a customer — unassigned, support_status=open, customer seeded as the participant, which is exactly the shape support_queue() selects on. Call it from an order/ticket flow instead of writing the row: a support conversation with a blank status or no participant is one no operator will ever be shown.",
      "instead_of": [
        "stapel_chat.models.Conversation.objects.create"
      ]
    },
    "post_message": {
      "kind": "factory",
      "intent": "THE send path, and the only sanctioned way a message comes into being: allocates the next per-conversation seq under a select_for_update row lock, persists the row and writes the chat.message outbox event in ONE transaction (mutate_and_emit), schedules best-effort realtime fan-out on_commit, retries on a seq collision. Inserting a Message with a seq you chose yourself bypasses the counter, breaks the gapless total order that anchor-paginated history and realtime resume both anchor on, and emits nothing at all.",
      "instead_of": [
        "stapel_chat.models.Message.objects.create"
      ]
    },
    "mark_read": {
      "kind": "gate_function",
      "intent": "Advance a participant's read marker to upto_seq and never backwards — the update is filtered on last_read_seq__lt, and the return value says whether it actually moved. Assigning last_read_seq yourself lets an out-of-order client rewind the marker and resurrect already-read messages as unread.",
      "instead_of": [
        "stapel_chat.models.ConversationParticipant.objects.update"
      ]
    },
    "unread_count": {
      "kind": "factory",
      "intent": "Messages past a participant's marker that somebody else authored — system lines (null sender) and the participant's own messages are excluded, and that exclusion is the whole difference from a naive count: a hand-rolled seq__gt count lights the badge for your own message and for every \"operator assigned\" system line.",
      "instead_of": [
        "stapel_chat.models.Message.objects.count"
      ]
    },
    "support_queue": {
      "kind": "factory",
      "intent": "The operator queue as a queryset: unassigned support conversations still open or pending, oldest first. Pass qs= to pre-scope it (typically the SCOPE_PROVIDER filter) rather than re-deriving the filter — \"what is still waiting for an operator\" is policy, and a second copy of it drifts from the one assign_operator enforces."
    },
    "assign_operator": {
      "kind": "gate_function",
      "intent": "First-come claim of a support thread: re-reads the conversation under select_for_update, refuses a second operator with AlreadyAssigned, stays idempotent for the same one, adds the operator participant, emits chat.support.assigned and posts the system line. Two operators pressing \"take\" in the same second is the race this exists for — setting assigned_operator directly loses it and silently steals the thread."
    },
    "set_support_status": {
      "kind": "gate_function",
      "intent": "Move a support thread between open/pending/resolved and optionally post the system line marking the transition; refuses a direct/group conversation with NotSupport. This is the one to call for pending, which has no dedicated wrapper."
    },
    "resolve_support": {
      "kind": "gate_function",
      "intent": "Close a support thread — status=resolved plus the chat.support.resolved system line. Call the wrapper so \"resolved\" reads the same way in every deployment's transcript and no thread is closed without a marker in its history."
    },
    "reopen_support": {
      "kind": "gate_function",
      "intent": "Reopen a resolved thread back to open with the chat.support.reopened system line. Note it re-enters support_queue() only while no operator is assigned — a reopened thread stays with its previous operator by design."
    }
  },
  "axes": {
    "CHAT_KINDS": {
      "business_label": "Which conversation types are offered",
      "summary": "The enabled subset of direct (1:1), group and support threads. Drop \"support\" to run a pure peer-to-peer chat with no operator queue or assignment; drop \"group\" to allow only 1:1 direct threads. Creating (or, for support, operating on) a kind that is not enabled is refused. Values: any non-empty subset of direct, group, support.",
      "behavior": "A create/operate request for a kind not in CHAT_KINDS is rejected (400); the support queue/assign/resolve/reopen endpoints answer 400 when \"support\" is not enabled."
    },
    "ATTACHMENTS": {
      "business_label": "Allow message attachments",
      "summary": "When on, a message may carry a list of opaque attachment keys (the files live in the host's CDN/storage; the module stores only the keys). When off, any message with an attachment is rejected. Text is always allowed.",
      "behavior": "off: a message with a non-empty attachments list is rejected (400); on: attachment keys are accepted and stored on the message."
    },
    "MAX_BODY_LENGTH": {
      "business_label": "Maximum message length",
      "summary": "Hard cap, in characters, on a single text message body. A longer body is rejected before it is stored. Raise it for long-form support replies, lower it for terse chat.",
      "behavior": "A body longer than MAX_BODY_LENGTH characters is rejected (400) at the API boundary."
    }
  }
}
