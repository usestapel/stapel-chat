{
  "module": "stapel-chat",
  "version": "0.7.1",
  "provides": "Conversations and messaging over WebSocket: direct (1:1, idempotent by participant pair), group and support threads on one model; realtime send/edit/delete with a monotonic per-conversation seq for order and a separate revision sequence for resume, so an edit or a deletion made while a client was offline reaches it on reconnect; deletion is a tombstone the id keeps arriving under, so client caches and offline databases learn what to purge; attachments carry the render metadata a bubble needs on first paint (aspect, byte size, a 16px base64 thumbnail, voice duration and waveform, document mime and extension) behind an OPEN type registry; typing and activity states, read and delivery receipts as ephemeral signals; a live inbox stream so the conversation list does not poll either; anchor-paginated history and lists; and a support layer (queue, first-come assignment, open/pending/resolved with reopen).",
  "axes": [
    {
      "key": "CHAT_KINDS",
      "kind": "list",
      "default": [
        "direct",
        "group",
        "support"
      ],
      "group": "chat.kinds",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "A create/operate request for a kind not in CHAT_KINDS is rejected (400); the support queue/assign/resolve/reopen endpoints answer 400 when \"support\" is not enabled."
      },
      "curated": {
        "summary": "The enabled subset of direct (1:1), group and support threads. Drop \"support\" to run a pure peer-to-peer chat with no operator queue or assignment; drop \"group\" to allow only 1:1 direct threads. Creating (or, for support, operating on) a kind that is not enabled is refused. Values: any non-empty subset of direct, group, support.",
        "business_label": "Which conversation types are offered"
      }
    },
    {
      "key": "ATTACHMENTS",
      "kind": "bool",
      "default": true,
      "group": "chat.attachments",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "off: a message with a non-empty attachments list is rejected (400); on: attachment keys are accepted and stored on the message."
      },
      "curated": {
        "summary": "When on, a message may carry attachments — an opaque CDN key plus the render metadata a bubble needs to paint immediately (the bytes live in the host's CDN; the module stores only the descriptor). When off, any message with an attachment is rejected. Text is always allowed.",
        "business_label": "Allow message attachments"
      }
    },
    {
      "key": "MAX_BODY_LENGTH",
      "kind": "enum",
      "default": 4000,
      "group": "chat.limits",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "A body longer than MAX_BODY_LENGTH characters is rejected (400) at the API boundary."
      },
      "curated": {
        "summary": "Hard cap, in characters, on a single text message body. A longer body is rejected before it is stored. Raise it for long-form support replies, lower it for terse chat.",
        "business_label": "Maximum message length"
      }
    },
    {
      "key": "ATTACHMENT_TYPES",
      "kind": "enum",
      "default": {},
      "group": "chat.attachments",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "Merged over the builtins; a value of None removes a builtin. A message naming an unregistered type is rejected (400)."
      },
      "curated": {
        "summary": "The catalogue of attachment kinds this deployment accepts. Out of the box: photos, animated GIFs, video, voice/audio and documents — named exactly as stapel-cdn names them, so the two never drift into two vocabularies for one thing. It is an open list: a product that wants stickers, locations or contact cards adds them in configuration and the API accepts them the same day.",
        "business_label": "Which kinds of attachment exist"
      }
    },
    {
      "key": "ACTIVITY_STATES",
      "kind": "enum",
      "default": {},
      "group": "chat.realtime",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "Merged over the builtins; None removes one. An unregistered state is refused (400 over HTTP, an error frame over the socket)."
      },
      "curated": {
        "summary": "What one participant can be shown to be doing right now — typing, recording a voice message, sending a video, uploading a file. Also an open list: \"choosing a sticker\" is a configuration entry, not a release. Each state carries how long it stays on screen without a refresh, so an indicator disappears on its own when someone closes their laptop mid-word.",
        "business_label": "Which live activity indicators exist"
      }
    },
    {
      "key": "ATTACHMENT_METADATA",
      "kind": "enum",
      "default": "cdn",
      "group": "chat.attachments",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "cdn: the describe answer is merged over the client's fields; an unreachable CDN leaves the client's values in place rather than failing the send. client: no call is made."
      },
      "curated": {
        "summary": "\"cdn\" (the default) asks the CDN to describe a message's attachments in one call as it is sent, so thumbnails, aspect ratios, durations and waveforms are computed once by the service that owns the file. \"client\" trusts what the sender's app supplied and makes no call — faster, and only sound where the sender is trusted.",
        "business_label": "Where attachment previews come from"
      }
    },
    {
      "key": "MAX_ATTACHMENTS",
      "kind": "enum",
      "default": 10,
      "group": "chat.attachments",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "A message with more attachments than this is rejected (400)."
      },
      "curated": {
        "summary": "How many files one message may carry.",
        "business_label": "Attachments per message"
      }
    },
    {
      "key": "MAX_PREVIEW_B64_BYTES",
      "kind": "enum",
      "default": 4096,
      "group": "chat.limits",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "An inline preview over this size, or one that is not an image data URI, is rejected (400)."
      },
      "curated": {
        "summary": "Ceiling on the tiny preview image that travels inside the message itself (the blur-up thumbnail, the video poster, the voice waveform). Matches the CDN's own budget deliberately — a larger number here would accept what the authority already refused. The cap exists because these are bytes from one user rendered on another user's screen, and they multiply by the number of attachments on the page.",
        "business_label": "Maximum inline preview size"
      }
    },
    {
      "key": "EDIT_WINDOW_S",
      "kind": "enum",
      "default": 0,
      "group": "chat.limits",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "An edit after the window is rejected (400); 0 disables the deadline entirely."
      },
      "curated": {
        "summary": "Seconds after posting during which the author may still correct their own message. Zero — the default — means there is no deadline. Deleting is unaffected: a message can always be taken back, and taking it back leaves a tombstone rather than a hole.",
        "business_label": "How long a message stays editable"
      }
    },
    {
      "key": "MODERATION_TARGET_TYPE",
      "kind": "enum",
      "default": "chat_message",
      "group": "chat.moderation",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "Registered at app-ready and only into a GAP — a host that already declared this type keeps its own policy, since the runtime layer outranks settings. Without stapel-moderation installed the setting has no effect."
      },
      "curated": {
        "summary": "The stapel-moderation target type registered for chat messages. \"chat_message\" (the default) makes a message reportable: a complaint opens a case on the real message, read through chat.moderation_content, instead of on the reporter's screenshot. \"\" registers nothing.",
        "business_label": "Whether members can report chat messages"
      }
    },
    {
      "key": "SUBJECT_TYPES",
      "kind": "enum",
      "default": {},
      "group": "chat.subjects",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "A conversation may only be created with a subject type that is registered (400 otherwise). For a DIRECT thread the subject is part of the thread's identity — the same two people asking about a different subject get a different thread. A declared type whose card_function is missing is a boot error (E020); one that is merely unreachable from this process is a warning (W005) and its conversations render with meta_status=partial."
      },
      "curated": {
        "summary": "The subject types this deployment offers, each naming the batched comm Function that renders one into a card. Ships EMPTY: a generic chat has no subject types, and the obvious one (\"listing\") belongs to whoever owns listings, not to messaging. A marketplace declares {\"listing\": {\"card_function\": \"classified.subject_cards\"}}. Merge over builtins; None removes.",
        "business_label": "What conversations can be about"
      }
    },
    {
      "key": "SUBJECT_CARD_TIMEOUT_S",
      "kind": "enum",
      "default": 2.0,
      "group": "chat.subjects",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "On timeout the conversation is returned with subject.meta_status=\"partial\" and meta_reason=\"card_function_failed\"; the thread and its messages are unaffected."
      },
      "curated": {
        "summary": "Seconds to wait for a subject type's card_function before rendering the conversation without its card. A header is never worth blocking a thread on.",
        "business_label": "How long to wait for a conversation's header card"
      }
    },
    {
      "key": "BLOCK_ENFORCEMENT",
      "kind": "enum",
      "default": "auto",
      "group": "chat.blocks",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "Direct threads only. A blocked send answers 403 with an error key that names no block and discloses no direction; a provider that is present and FAILING answers 503, never a delivered message, because an outage is not consent. Group threads are not checked (a room is somebody else's convening) and support threads never are (an operator is not a peer, and a customer must not be able to mute the help desk)."
      },
      "curated": {
        "summary": "Whether a block stops a send into an EXISTING direct thread. \"auto\" enforces when a block provider is reachable and does not when there is none (announced at every boot by W003). \"required\" is for a deployment that has blocks and refuses to run without them: an unreachable provider is a boot error and a 503 at send. \"off\" is a deliberate decision on the record (W004). Blocking elsewhere in the fleet only stops NEW conversations — a block that does not reach the send path is half a block.",
        "business_label": "Whether a blocked user can still send"
      }
    },
    {
      "key": "BLOCK_FUNCTION",
      "kind": "enum",
      "default": "profiles.relationships",
      "group": "chat.blocks",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "Consulted once per send into a direct thread, batched over every other party. If the name is not registered anywhere, behaviour follows BLOCK_ENFORCEMENT."
      },
      "curated": {
        "summary": "The comm Function consulted for blocks, by name — never an import, so chat and the block owner stay independently deployable. Default \"profiles.relationships\" (stapel-profiles): {\"pairs\": [[a, b], ...]} in, {\"blocked\": [[a, b], ...]} out, blocked in either direction. Repoint it at a different provider without forking.",
        "business_label": "Who is asked whether two users are blocked"
      }
    },
    {
      "key": "BLOCK_TIMEOUT_S",
      "kind": "enum",
      "default": 2.0,
      "group": "chat.blocks",
      "gates": {
        "operations": [],
        "co_gates": [],
        "behavior": "A timeout is an outage: the send answers 503 and the message is not delivered. Failing open would deliver a message to somebody who blocked the sender, and they would never know why."
      },
      "curated": {
        "summary": "Seconds to wait for the block provider before treating the call as failed. Unlike a subject card, a timeout here is never shrugged off.",
        "business_label": "How long to wait for the block answer"
      }
    }
  ],
  "extension_points": [
    {
      "name": "SCOPE_PROVIDER",
      "kind": "dotted_path",
      "summary": "Swap how the opaque scope_key (workspace/org/tenant) is resolved from the request and enforced on querysets — the default is a single global scope; a host may return the active workspace_id so conversations are partitioned per tenant."
    },
    {
      "name": "chat.message",
      "kind": "comm_event",
      "summary": "Emitted (transactionally, via the outbox) whenever a message is appended. Realtime delivery, search indexing and notifications subscribe without any coupling in the engine."
    },
    {
      "name": "chat.support.assigned",
      "kind": "comm_event",
      "summary": "Emitted when a support conversation is claimed by an operator — routing and operator-notification layers subscribe."
    },
    {
      "name": "serializer_seams",
      "kind": "class_override",
      "summary": "Every view declares request/response serializer seams (SerializerSeamMixin) — subclass the view, override the attribute, remount the URL."
    },
    {
      "name": "ATTACHMENT_TYPES",
      "kind": "merge_registry",
      "summary": "The OPEN attachment-type registry — builtins (image, gif, video, voice, file) merged under the setting and then under register_attachment_type(), later wins, None removes. Stickers are the named next type: adding one is a settings line, not a contract change."
    },
    {
      "name": "ACTIVITY_STATES",
      "kind": "merge_registry",
      "summary": "The OPEN activity registry — typing, recording audio, sending video, uploading file, idle — with the same merge semantics. \"Choosing a sticker\" is a settings line."
    },
    {
      "name": "chat.message.edited",
      "kind": "comm_event",
      "summary": "Emitted when an author replaces a message body. Carries a fresh revision sequence, which is what makes the edit reachable by a client that was offline when it happened."
    },
    {
      "name": "chat.message.deleted",
      "kind": "comm_event",
      "summary": "Emitted when a message becomes a tombstone. The row is not removed — the id keeps being delivered, stripped, so every client cache learns which one to purge."
    },
    {
      "name": "MODERATION_TARGET_TYPE",
      "kind": "merge_registry",
      "summary": "The stapel-moderation target type registered for chat messages (chat_message) when that module is installed and no host declared the type first — the host's declaration always wins."
    }
  ],
  "surface": [
    {
      "name": "announce_activity",
      "kind": "gate_function",
      "path": "stapel_chat.services.announce_activity",
      "intent": "Fan out \"typing…\" (or any registered state) to everyone watching, with the TTL that makes the indicator expire on its own — the only design that survives a tab closed mid-word. Nothing is persisted; someone who is not connected is owed nothing."
    },
    {
      "name": "assign_operator",
      "kind": "gate_function",
      "path": "stapel_chat.services.assign_operator",
      "intent": "First-come claim of a support thread under a row lock: refuses a second operator, stays idempotent for the same one, adds the participant, emits the event and posts the system line. Two operators pressing \"take\" in the same second is the race this exists for."
    },
    {
      "name": "conversation_participants",
      "kind": "predicate",
      "path": "stapel_chat.services.conversation_participants",
      "intent": "Answer, in one batched call, who is a party to each of these conversations — with the thread's kind and subject. Reach for it instead of storing your own copy of the two party ids on your own row: a copy nothing can refresh is a copy that goes stale the moment a participant changes here. Every id asked about is answered, including one that names nothing (exists: false), because a caller holding a dead id must be told rather than left to infer it from an absence.",
      "instead_of": [
        "stapel_chat.models.ConversationParticipant.objects.filter"
      ]
    },
    {
      "name": "create_direct",
      "kind": "factory",
      "path": "stapel_chat.services.create_direct",
      "intent": "Get-or-create the 1:1 thread between two users — idempotent by an order-independent key over (scope, both ids), with the create race decided by a partial unique constraint. A hand-rolled get_or_create has nothing unique to key on and quietly hands the two users two different threads.",
      "instead_of": [
        "stapel_chat.models.Conversation.objects.get_or_create"
      ]
    },
    {
      "name": "create_group",
      "kind": "factory",
      "path": "stapel_chat.services.create_group",
      "intent": "Create a group thread and seed its participant rows in one call (owner first, ids deduped). A Conversation row created by hand has no participants, and the list endpoint filters on participants — so nobody can see it.",
      "instead_of": [
        "stapel_chat.models.Conversation.objects.create"
      ]
    },
    {
      "name": "create_support",
      "kind": "factory",
      "path": "stapel_chat.services.create_support",
      "intent": "Open a support thread for a customer — unassigned, status open, customer seeded as participant, which is exactly the shape the queue selects on. A support conversation with a blank status or no participant is one no operator will ever be shown.",
      "instead_of": [
        "stapel_chat.models.Conversation.objects.create"
      ]
    },
    {
      "name": "delete_message",
      "kind": "gate_function",
      "path": "stapel_chat.services.delete_message",
      "intent": "Turn a message into a TOMBSTONE — content emptied, deleted_at stamped, fresh revision sequence — instead of removing the row. Deleting the row destroys the message on the server and leaves every copy on every other device, because nothing tells those devices which id ceased to exist.",
      "instead_of": [
        "stapel_chat.models.Message.objects.delete"
      ]
    },
    {
      "name": "edit_message",
      "kind": "gate_function",
      "path": "stapel_chat.services.edit_message",
      "intent": "Replace a message body, stamp edited_at, and — the part a hand-written update always misses — take a FRESH revision sequence. Anchored on the message's own seq an edit sits behind a cursor every client has already acknowledged, so nobody who was offline would ever see it.",
      "instead_of": [
        "stapel_chat.models.Message.objects.update"
      ]
    },
    {
      "name": "erase_user_messages",
      "kind": "gate_function",
      "path": "stapel_chat.services.erase_user_messages",
      "intent": "The erasure path: every message a user wrote becomes an anonymous tombstone, sequences reserved as one block per thread. Content is destroyed either way — only this way does the destruction reach the devices still displaying it.",
      "instead_of": [
        "stapel_chat.models.Message.objects.delete"
      ]
    },
    {
      "name": "journal_rows",
      "kind": "factory",
      "path": "stapel_chat.services.journal_rows",
      "intent": "The replay source a resuming socket reads: rows past the client's cursor by REVISION sequence. Filtering on the message's own seq is the bug this exists to prevent — it silently drops every edit and tombstone that happened while the client was away."
    },
    {
      "name": "mark_delivered",
      "kind": "gate_function",
      "path": "stapel_chat.services.mark_delivered",
      "intent": "Advance the DELIVERY marker (never backwards) and fan out its receipt. Delivered is a weaker fact than read and a real one, and only the client knows the difference — which is why it is an explicit call, not something inferred from an open socket.",
      "instead_of": [
        "stapel_chat.models.ConversationParticipant.objects.update"
      ]
    },
    {
      "name": "mark_read",
      "kind": "gate_function",
      "path": "stapel_chat.services.mark_read",
      "intent": "Advance a read marker to upto_seq and never backwards, and fan out the receipt when it moves. Assigning last_read_seq yourself lets an out-of-order client rewind the marker, resurrect read messages as unread, and tells nobody's screen anything.",
      "instead_of": [
        "stapel_chat.models.ConversationParticipant.objects.update"
      ]
    },
    {
      "name": "moderation_content",
      "kind": "gate_function",
      "path": "stapel_chat.services.moderation_content",
      "intent": "Read one message's live content for an external moderation module — body, attachment KEYS, author, conversation. Fetched when it is looked at, so a card shows the message as it is now; a deleted or erased message raises MessageNotFound instead of answering with the empty body a tombstone leaves, because a blank card looks exactly like a message that said nothing.",
      "instead_of": [
        "stapel_chat.models.Message.objects.get"
      ]
    },
    {
      "name": "post_message",
      "kind": "factory",
      "path": "stapel_chat.services.post_message",
      "intent": "THE send path, and the only sanctioned way a message comes into being: allocates the next seq under a row lock, persists the row and the chat.message outbox event in ONE transaction, normalizes and CDN-enriches the attachments once, fans out to the sockets on commit, and returns the existing row when a client_msg_id is retried. Inserting a Message with your own seq bypasses the counter, breaks the order history paging and realtime resume both anchor on, emits nothing, and reaches no open socket.",
      "instead_of": [
        "stapel_chat.models.Message.objects.create"
      ]
    },
    {
      "name": "presence_for",
      "kind": "factory",
      "path": "stapel_chat.services.presence_for",
      "intent": "Resolve, in ONE query, whether each participant of these conversations is connected right now and when they were last seen. Reach for it whenever you render a thread header or an inbox: presence is a fact about the OTHER person's own sockets, and a client that derives it from its own socket state is stating that its network is up while labelling it with somebody else's name — the defect this call exists to make impossible.",
      "instead_of": [
        "stapel_chat.models.UserPresence.objects.filter"
      ]
    },
    {
      "name": "reopen_support",
      "kind": "gate_function",
      "path": "stapel_chat.services.reopen_support",
      "intent": "Reopen a resolved thread with its system line. It re-enters the queue only while no operator is assigned — a reopened thread stays with its previous operator by design."
    },
    {
      "name": "resolve_support",
      "kind": "gate_function",
      "path": "stapel_chat.services.resolve_support",
      "intent": "Close a support thread — status resolved plus its system line — so \"resolved\" reads the same way in every deployment's transcript and no thread closes without a marker in its history."
    },
    {
      "name": "set_support_status",
      "kind": "gate_function",
      "path": "stapel_chat.services.set_support_status",
      "intent": "Move a support thread between open/pending/resolved and optionally post the system line marking the transition. The one to call for pending, which has no dedicated wrapper."
    },
    {
      "name": "subject_cards_for",
      "kind": "factory",
      "path": "stapel_chat.services.subject_cards_for",
      "intent": "Resolve the subject cards for a whole list of conversations in ONE call per subject type, by asking each type's registered card_function. Reach for it whenever you render more than one conversation: resolving per conversation turns a fifty-row inbox into fifty round trips, which is the entire reason the provider contract is a batch.",
      "instead_of": [
        "stapel_core.comm.call"
      ]
    },
    {
      "name": "support_queue",
      "kind": "factory",
      "path": "stapel_chat.services.support_queue",
      "intent": "The operator queue as a queryset: unassigned support threads still open or pending, oldest first. Pass qs= to pre-scope it rather than re-deriving the filter — a second copy of \"what is still waiting\" drifts from the one assign_operator enforces."
    },
    {
      "name": "unread_count",
      "kind": "factory",
      "path": "stapel_chat.services.unread_count",
      "intent": "Messages past a participant's marker that somebody else authored — system lines, your own messages and tombstones excluded. A naive seq__gt count lights the badge for your own message, for every system line, and for a message deleted before you got to it: a badge nobody can clear.",
      "instead_of": [
        "stapel_chat.models.Message.objects.count"
      ]
    }
  ],
  "operations_total": 13,
  "requires": [
    {
      "module": "stapel-core",
      "optional": false,
      "reason": "comm bus (chat.message / chat.support.assigned emits via the outbox), JWT authentication (HTTP + optional Channels), AppSettings config layer, anchor pagination"
    },
    {
      "module": "stapel-auth",
      "optional": true,
      "reason": "every endpoint requires an authenticated user (IsAuthenticated); stapel-auth is the shelf's session issuer — any stapel-core-compatible JWT issuer satisfies the check"
    },
    {
      "module": "stapel-cdn",
      "optional": true,
      "reason": "attachment render metadata — chat calls cdn.describe_many by comm to denormalize aspect, byte size, the micro-thumbnail, the video poster and the voice waveform once at send time, and shares one type vocabulary with its media-kind registry. Optional: without it a message still sends carrying whatever the client supplied."
    },
    {
      "module": "stapel-realtime",
      "optional": false,
      "reason": "the WebSocket substrate — both consumers, the v1 wire envelope, the stream-key canon, the fail-closed authorize seam, the close-code set and the origin guard. Not optional: realtime is the canonical path, and chat previously carried its own duplicate socket implementation."
    }
  ]
}
