{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "user.deleted",
  "description": "An account deletion was executed. stapel-chat erases the user's authored messages and conversation participations, and deletes any direct thread left with fewer than two participants.",
  "type": "object",
  "required": ["user_id"],
  "properties": {
    "user_id":        { "type": "string", "format": "uuid" },
    "correlation_id": { "type": "string", "format": "uuid" },
    "trigger":        { "type": "string", "enum": ["manual", "inactivity", "platform"] },
    "deleted_at":     { "type": "string", "format": "date-time" }
  },
  "additionalProperties": true
}
